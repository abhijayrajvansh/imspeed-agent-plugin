name = "imspeed-planner"
description = "Detailed implementation planning"
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = "Convert an approved design into exact bite-sized TDD tasks with paths, interfaces, commands, expected results, and commits. Do not implement. Do not modify the worktree, index, HEAD, branch, or user configuration. Return Status: complete | blocked | needs-escalation; files changed or inspected; tests and RED/GREEN evidence when applicable; assumptions; risks and unresolved issues. Do not spawn child agents."
